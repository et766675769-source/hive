<Window x:Class="MessageBoard.Shell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Message Board · 留言板"
        Height="900" Width="1440" MinHeight="560" MinWidth="880"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="#FAF9F7">

  <!-- 无边框 + 自绘标题栏：CaptionHeight 内为可拖拽区域，ResizeBorderThickness 保留边缘缩放 -->
  <WindowChrome.WindowChrome>
    <WindowChrome CaptionHeight="34" ResizeBorderThickness="6" CornerRadius="0"
                  GlassFrameThickness="0" UseAeroCaptionButtons="False" />
  </WindowChrome.WindowChrome>

  <Window.Resources>
    <SolidColorBrush x:Key="CaptionHoverBrush" Color="#EFEDE9" />
    <SolidColorBrush x:Key="CaptionLineBrush" Color="#E9E6E1" />

    <Style x:Key="CaptionButton" TargetType="Button">
      <Setter Property="Width" Value="46" />
      <Setter Property="Height" Value="34" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Foreground" Value="#6B7280" />
      <Setter Property="FontSize" Value="11" />
      <Setter Property="Focusable" Value="False" />
      <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bg" Background="{TemplateBinding Background}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource CaptionHoverBrush}" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="CloseCaptionButton" TargetType="Button" BasedOn="{StaticResource CaptionButton}">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bg" Background="{TemplateBinding Background}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bg" Property="Background" Value="#E5484D" />
                <Setter Property="Foreground" Value="White" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="34" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- 自绘标题栏：只有这一条，且随页面主题一起变色 -->
    <Border x:Name="TitleBar" Grid.Row="0" Background="#FAF9F7"
            BorderBrush="{DynamicResource CaptionLineBrush}" BorderThickness="0,0,0,1">
      <Grid>
        <TextBlock x:Name="TitleText" Text="Message Board · 留言板"
                   FontSize="11.5" Foreground="#6B7280"
                   VerticalAlignment="Center" Margin="14,0,0,0" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <Button x:Name="MinButton" Content="&#x2014;" Style="{StaticResource CaptionButton}" Click="Min_Click" />
          <Button x:Name="MaxButton" Content="&#x25A2;" Style="{StaticResource CaptionButton}" Click="Max_Click" />
          <Button x:Name="CloseButton" Content="&#x2715;" Style="{StaticResource CloseCaptionButton}" Click="Close_Click" />
        </StackPanel>
      </Grid>
    </Border>

    <Grid Grid.Row="1">
      <wv2:WebView2 x:Name="View" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
      <Border x:Name="Splash" Background="#FAF9F7">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
          <TextBlock Text="Message Board" FontSize="26" FontWeight="SemiBold" Foreground="#1E1E1E" HorizontalAlignment="Center" />
          <TextBlock Text="留言板 · 本地多 AI 协作黑板" FontSize="13" Foreground="#6B7280" Margin="0,10,0,0" HorizontalAlignment="Center" />
          <TextBlock x:Name="Status" Text="正在准备黑板…" FontSize="12" Foreground="#9CA3AF" Margin="0,22,0,0" HorizontalAlignment="Center" />
        </StackPanel>
      </Border>
    </Grid>
  </Grid>
</Window>
